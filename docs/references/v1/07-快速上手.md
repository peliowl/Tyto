# 快速上手

本文档介绍如何安装、初始化和使用 Tyto UI 组件库。

---

## 1. 安装

### 1.1 环境要求

- Python ≥ 3.12
- uv 包管理器

### 1.2 安装依赖

```bash
uv sync
```

### 1.3 开发依赖

```bash
uv sync --extra dev
```

包含：pytest, pytest-qt, pytest-xdist, hypothesis, ruff, pyright

---

## 2. 初始化主题引擎

使用任何 Tyto UI 组件前，必须先初始化 `ThemeEngine` 并激活一个主题：

```python
import sys
from PySide6.QtWidgets import QApplication
from tyto_ui_lib import ThemeEngine

app = QApplication(sys.argv)
app.setStyle("Fusion")  # 推荐使用 Fusion 基础样式

engine = ThemeEngine.instance()
engine.load_tokens()           # 加载内置 Light/Dark 令牌
engine.switch_theme("light")   # 激活 Light 主题
```

---

## 3. 使用组件

### 3.1 导入

所有公开组件均可从顶层包直接导入：

```python
from tyto_ui_lib import (
    # Atoms
    TButton, TCheckbox, TRadio, TRadioGroup,
    TInput, TSwitch, TTag,
    # Molecules
    TSearchBar, TBreadcrumb, BreadcrumbItem, TInputGroup,
    # Organisms
    TMessage, MessageManager, MessageType, TModal,
    # Core
    ThemeEngine, DesignTokenSet, load_tokens_from_file,
    # Common
    BaseWidget, HoverEffectMixin, ClickRippleMixin,
    FocusGlowMixin, DisabledMixin,
)
```

### 3.2 基本用法

```python
from PySide6.QtWidgets import QVBoxLayout, QWidget
from tyto_ui_lib import TButton, TInput, TCheckbox

window = QWidget()
layout = QVBoxLayout(window)

layout.addWidget(TInput(placeholder="Enter your name"))
layout.addWidget(TCheckbox("I agree to the terms"))
layout.addWidget(TButton("Submit", button_type=TButton.ButtonType.PRIMARY))

window.show()
```

### 3.3 主题切换

```python
from tyto_ui_lib import TSwitch, ThemeEngine

switch = TSwitch(checked=False)
switch.toggled.connect(
    lambda dark: ThemeEngine.instance().switch_theme("dark" if dark else "light")
)
```

---

## 4. Gallery 示例

项目内置了一个完整的组件预览画廊，展示所有组件的各种状态和交互效果：

```bash
uv run python examples/gallery.py
```

Gallery 包含以下演示区域：

| 区域 | 展示内容 |
|------|----------|
| Button | Primary / Default / Dashed / Text 四种类型，Loading 和 Disabled 状态 |
| Checkbox | Unchecked / Checked / Indeterminate 三种状态 |
| Radio | RadioGroup 互斥选择 |
| Input | 基本输入、可清除输入、密码输入 |
| Switch | 开/关/禁用状态 |
| Tag | 五种颜色类型、三种尺寸、可关闭 |
| SearchBar | 搜索栏组合组件 |
| Breadcrumb | 导航路径 |
| InputGroup | 输入框 + 按钮紧凑组合 |
| Message | 四种类型的全局消息触发 |
| Modal | 模态对话框打开/关闭 |

顶部工具栏提供 Dark Mode 开关，可实时切换 Light / Dark 主题。

---

## 5. 测试

```bash
# 运行全部测试
uv run pytest

# 并行运行
uv run pytest -n auto

# 运行特定组件测试
uv run pytest tests/test_atoms/test_button.py

# 类型检查
uv run pyright src/

# 代码检查
uv run ruff check src/ tests/
```

---

## 6. 公开 API 一览

| 模块 | 导出 |
|------|------|
| `tyto_ui_lib` | 所有公开类和函数的顶层入口 |
| `tyto_ui_lib.core` | `ThemeEngine`, `DesignTokenSet`, `TokenFileError`, `load_tokens_from_file` |
| `tyto_ui_lib.common` | `BaseWidget`, `HoverEffectMixin`, `ClickRippleMixin`, `FocusGlowMixin`, `DisabledMixin` |
| `tyto_ui_lib.components.atoms` | `TButton`, `TCheckbox`, `TInput`, `TRadio`, `TRadioGroup`, `TSwitch`, `TTag` |
| `tyto_ui_lib.components.molecules` | `TSearchBar`, `TBreadcrumb`, `BreadcrumbItem`, `TInputGroup` |
| `tyto_ui_lib.components.organisms` | `TMessage`, `MessageManager`, `MessageSlot`, `MessageType`, `TModal` |
