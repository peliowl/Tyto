{
  "enabled": true,
  "name": "低置信度决策拦截",
  "description": "当 Agent 遇到不确定的决策（置信度低于 80%）时，在执行写操作前提醒 Agent 必须先向用户提供解决方案进行决策，不得自行假设。",
  "version": "1",
  "when": {
    "type": "preToolUse",
    "toolTypes": [
      "write"
    ]
  },
  "then": {
    "type": "askAgent",
    "prompt": "在执行此写操作前，请自检：你对当前决策的置信度是否达到 80% 以上？如果你对实现方式、API 设计、样式选择或架构决策存在不确定性（置信度低于 80%），你必须先暂停写操作，向用户提供 2-3 个可选方案并说明各方案的优劣，等待用户决策后再继续。如果置信度达到 80% 以上，则正常继续执行。"
  }
}